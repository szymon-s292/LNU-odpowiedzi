public class Speed
{
    public static boolean compareSpeed ( int roadRunnerSpeed, int coyoteSpeed ) {
        if(coyoteSpeed > roadRunnerSpeed) return true;
        else return false;
    }
}
